name: substrate

networks:
  substrate_internal:
    driver: bridge

volumes:
  pg_data:
  kc_data:
  pgadmin_data:

services:

  postgres:
    build: ../infra/postgres
    container_name: substrate-postgres
    env_file: [../../env/infra.env]
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: ${POSTGRES_SUPERUSER}
      POSTGRES_PASSWORD: ${POSTGRES_SUPERUSER_PASSWORD}
      GRAPH_DB_USER: ${GRAPH_DB_USER}
      GRAPH_DB_PASSWORD: ${GRAPH_DB_PASSWORD}
      GRAPH_DB_NAME: ${GRAPH_DB_NAME}
      KC_DB_USER: ${KC_DB_USER}
      KC_DB_PASSWORD: ${KC_DB_PASSWORD}
      KC_DB_NAME: ${KC_DB_NAME}
    ports: ["5432:5432"]
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ../infra/postgres/init:/docker-entrypoint-initdb.d:ro
    networks: [substrate_internal]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_SUPERUSER} -d postgres"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: substrate-keycloak
    command: ["start-dev", "--import-realm"]
    env_file: [../../env/infra.env]
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/${KC_DB_NAME}
      KC_DB_USERNAME: ${KC_DB_USER}
      KC_DB_PASSWORD: ${KC_DB_PASSWORD}
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KC_BOOTSTRAP_ADMIN_USERNAME}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KC_BOOTSTRAP_ADMIN_PASSWORD}
      KC_HOSTNAME: ${KC_HOSTNAME}
      KC_HOSTNAME_STRICT: ${KC_HOSTNAME_STRICT}
      KC_HTTP_ENABLED: ${KC_HTTP_ENABLED}
      KC_HEALTH_ENABLED: "true"
      KC_METRICS_ENABLED: "true"
    ports: ["8080:8080"]
    volumes:
      - kc_data:/opt/keycloak/data
      - ../infra/keycloak:/opt/keycloak/data/import:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks: [substrate_internal]
    healthcheck:
      # /health/ready ships with every Keycloak 26+. Avoids the realm handler's
      # HostnameV2Provider path entirely so we don't need a Host header.
      # /health/ready returns 200 once the realm import is complete and the
      # HTTP listener is accepting traffic.
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/9000 && printf 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && head -c 15 <&3 | grep -q '200'"]
      interval: 10s
      timeout: 5s
      retries: 30
    restart: unless-stopped

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: substrate-pgadmin
    env_file: [../../env/infra.env]
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
      PGADMIN_CONFIG_SERVER_MODE: "False"
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: "False"
      GRAPH_DB_USER: ${GRAPH_DB_USER}
      GRAPH_DB_PASSWORD: ${GRAPH_DB_PASSWORD}
      GRAPH_DB_NAME: ${GRAPH_DB_NAME}
      KC_DB_USER: ${KC_DB_USER}
      KC_DB_PASSWORD: ${KC_DB_PASSWORD}
      KC_DB_NAME: ${KC_DB_NAME}
      POSTGRES_SUPERUSER: ${POSTGRES_SUPERUSER}
      POSTGRES_SUPERUSER_PASSWORD: ${POSTGRES_SUPERUSER_PASSWORD}
    ports: ["5050:80"]
    entrypoint: /custom-entrypoint.sh
    volumes:
      - pgadmin_data:/var/lib/pgadmin
      - ../infra/pgadmin/servers.json:/pgadmin4/servers.json:ro
      - ../infra/pgadmin/entrypoint.sh:/custom-entrypoint.sh:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks: [substrate_internal]
    restart: unless-stopped

  gateway:
    build:
      context: ../..
      dockerfile: services/gateway/Dockerfile
    container_name: substrate-gateway
    env_file: [../../env/platform.env, ../../env/infra.env, ../../env/llm.env]
    environment:
      SERVICE_NAME: gateway
      GRAPH_SERVICE_URL: http://graph:8082
      INGESTION_SERVICE_URL: http://ingestion:8081
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_ISSUER: ${KEYCLOAK_ISSUER}
      KEYCLOAK_REALM: ${KEYCLOAK_REALM}
      AUTH_DISABLED: ${AUTH_DISABLED}
      CORS_ORIGINS: ${CORS_ORIGINS}
      DATABASE_URL: postgresql+asyncpg://${GRAPH_DB_USER}:${GRAPH_DB_PASSWORD}@postgres:5432/${GRAPH_DB_NAME}
      APP_PORT: "8080"
    ports: ["8180:8080"]
    depends_on:
      postgres: { condition: service_healthy }
      keycloak: { condition: service_started }
    networks: [substrate_internal]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 10s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  ingestion:
    build:
      context: ../..
      dockerfile: services/ingestion/Dockerfile
    container_name: substrate-ingestion
    env_file: [../../env/platform.env, ../../env/infra.env, ../../env/llm.env]
    environment:
      SERVICE_NAME: ingestion
      DATABASE_URL: postgresql+asyncpg://${GRAPH_DB_USER}:${GRAPH_DB_PASSWORD}@postgres:5432/${GRAPH_DB_NAME}
      EMBEDDING_URL: ${EMBEDDING_URL}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL}
      EMBEDDING_DIM: ${EMBEDDING_DIM}
      LLM_API_KEY: ${LLM_API_KEY}
      APP_PORT: "8081"
    ports: ["8181:8081"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres: { condition: service_healthy }
    networks: [substrate_internal]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8081/health')"]
      interval: 10s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  graph:
    build:
      context: ../..
      dockerfile: services/graph/Dockerfile
    container_name: substrate-graph
    env_file: [../../env/platform.env, ../../env/infra.env, ../../env/llm.env]
    environment:
      SERVICE_NAME: graph
      FLYWAY_URL: jdbc:postgresql://postgres:5432/${GRAPH_DB_NAME}
      FLYWAY_USER: ${GRAPH_DB_USER}
      FLYWAY_PASSWORD: ${GRAPH_DB_PASSWORD}
      DATABASE_URL: postgresql+asyncpg://${GRAPH_DB_USER}:${GRAPH_DB_PASSWORD}@postgres:5432/${GRAPH_DB_NAME}
      EMBEDDING_URL: ${EMBEDDING_URL}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL}
      EMBEDDING_DIM: ${EMBEDDING_DIM}
      DENSE_LLM_URL: ${DENSE_LLM_URL}
      DENSE_LLM_MODEL: ${DENSE_LLM_MODEL}
      LLM_API_KEY: ${LLM_API_KEY}
      APP_PORT: "8082"
    ports: ["8182:8082"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres: { condition: service_healthy }
    networks: [substrate_internal]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8082/health')"]
      interval: 10s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  frontend:
    build:
      context: ../..
      dockerfile: apps/frontend/Dockerfile
      args:
        GITHUB_TOKEN: ""
        # Vite bakes these at build time. All browser-facing URLs must
        # use host-published ports (browser cannot resolve the internal
        # substrate_internal DNS). Runtime env has no effect here.
        VITE_KEYCLOAK_URL: "http://localhost:8080"
        VITE_KEYCLOAK_REALM: "${KEYCLOAK_REALM}"
        VITE_KEYCLOAK_CLIENT_ID: "substrate-frontend"
    container_name: substrate-frontend
    ports: ["3535:3000"]
    depends_on:
      gateway: { condition: service_started }
    networks: [substrate_internal]
    restart: unless-stopped
