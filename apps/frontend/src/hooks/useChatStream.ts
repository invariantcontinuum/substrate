// Streaming reducer arrives in Task 11. This stub keeps the import path
// stable so ChatPage compiles before the SSE wiring lands.
export function useChatStream(_threadId: string | null): void {
  // intentionally empty
}
