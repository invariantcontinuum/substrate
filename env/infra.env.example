# Postgres
POSTGRES_VERSION=16
POSTGRES_SUPERUSER=postgres
POSTGRES_SUPERUSER_PASSWORD=change-me

# Substrate graph DB credentials (only substrate_graph exists after Phase 7)
GRAPH_DB_USER=substrate_graph
GRAPH_DB_PASSWORD=change-me
GRAPH_DB_NAME=substrate_graph

# Keycloak admin + import
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=change-me
KC_HOSTNAME=localhost
KC_HOSTNAME_STRICT=false
KC_HTTP_ENABLED=true

# pgadmin
PGADMIN_DEFAULT_EMAIL=admin@substrate.local
PGADMIN_DEFAULT_PASSWORD=change-me
