# Shared application settings (non-URL). URL-bound vars live in env/overlays/<mode>.env.
SERVICE_LOG_PRETTY=0
GATEWAY_PORT=8080
INGESTION_PORT=8081
GRAPH_PORT=8082

# Keycloak (internal container DNS — identical in dev and prod).
KEYCLOAK_REALM=substrate
KEYCLOAK_URL=http://keycloak:8080
