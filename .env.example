# Compose sources all three profiles below plus .env
# make bootstrap materializes env/*.env from env/*.env.example if missing
# and concatenates them into a top-level .env for docker compose.
# Do NOT commit .env.
