# The top-level .env is generated by scripts/set-env.sh (invoked from
# `make configure MODE=dev|prod [DOMAIN=foo.com]`). It is the concatenation
# of env/platform.env, env/infra.env, env/llm.env, and
# env/overlays/<mode>.env. Do NOT edit .env directly; re-run configure.
